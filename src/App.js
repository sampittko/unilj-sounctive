import { AppBar, Toolbar, Typography } from "@material-ui/core"

import React from 'react';

function App() {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h6">
          Sounctive
        </Typography>
      </Toolbar>
    </AppBar>
  );
}

export default App;
